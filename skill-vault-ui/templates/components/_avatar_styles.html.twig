{# Shared CSS for the components/_avatar.html.twig macro. Include once per page. #}
<style>
    .group-avatar {
        display: flex;
        align-items: center;
        justify-content: center;
        flex-shrink: 0;
        color: #fff;
        box-shadow: 0 1px 3px rgba(15, 27, 51, 0.1);
    }

    .avatar-size-sm { width: 40px; height: 40px; border-radius: 10px; }
    .avatar-size-sm svg { width: 20px; height: 20px; }

    .avatar-size-md { width: 46px; height: 46px; border-radius: 11px; }
    .avatar-size-md svg { width: 23px; height: 23px; }

    .avatar-size-lg { width: 64px; height: 64px; border-radius: 16px; }
    .avatar-size-lg svg { width: 30px; height: 30px; }

    .avatar-color-blue { background: #2563eb; }
    .avatar-color-green { background: #16a34a; }
    .avatar-color-purple { background: #7c3aed; }
    .avatar-color-orange { background: #f97316; }
    .avatar-color-red { background: #e11d48; }
    .avatar-color-teal { background: #0d9488; }
    .avatar-color-indigo { background: #4f46e5; }
    .avatar-color-yellow { background: #ca8a04; }
    .avatar-color-slate { background: #475569; }
    .avatar-color-cyan { background: #0891b2; }
</style>
