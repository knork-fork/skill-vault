{% extends 'base.html.twig' %}

{% block title %}Sign up unavailable - Skill Vault{% endblock %}

{% block body %}
    <div class="auth-shell">
        <div class="card">
            <h1>Sign up unavailable</h1>
            <p style="color: var(--text-muted);">{{ message }}</p>
            <a href="{{ path('app_login') }}" class="btn btn-secondary">Back to log in</a>
        </div>
    </div>
{% endblock %}
